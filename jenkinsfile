pipeline {
    agent any
    tools {
        jdk 'jdk17'
        maven 'maven3'
    }
    environment {
        SCANNER_HOME = tool 'sonar-scanner'
        NEXUS_REGISTRY = 'http://172.17.7.99:8082'  // UPDATE: Your Nexus IP
        DOCKER_IMAGE_NAME = 'boardgame-app'
        K8S_NAMESPACE = 'boardgame'
        K8S_MASTER = 'https://172.17.7.95:6443'  // UPDATE: Your K8s master IP
        APP_VERSION = "${BUILD_NUMBER}"
    }
    stages {
        stage('Git Checkout') {
            steps {
                git branch: 'main', credentialsId: 'git-cred', url: 'https://github.com/killsmokker/ak-cicd.git'
            }
        }
        
        stage('Compile') {
            steps {
                sh "mvn compile"
            }
        }
        
        stage('Test') {
            steps {
                sh "mvn test"
                // Archive test results
                junit '**/target/surefire-reports/*.xml'
            }
        }
        
        stage('File System Scan') {
            steps {
                sh "trivy fs --format table -o trivy-fs-report.html ."
                archiveArtifacts 'trivy-fs-report.html'
            }
        }
        
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('sonar') {
                    sh """ 
                        $SCANNER_HOME/bin/sonar-scanner \
                        -Dsonar.projectName=BoardGame \
                        -Dsonar.projectKey=BoardGame \
                        -Dsonar.java.binaries=target/classes \
                        -Dsonar.sources=src/main/java \
                        -Dsonar.tests=src/test/java \
                        -Dsonar.junit.reportsPath=target/surefire-reports \
                        -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
                    """
                }
            }
        }
        
        stage('Quality Gate') {
            steps {
                script {
                    waitForQualityGate abortPipeline: true, credentialsId: 'sonar-token'
                }
            }
        }
        
        stage('Build Package') {
            steps {
                sh "mvn clean package -DskipTests"
                archiveArtifacts 'target/*.jar'
            }
        }
        
        stage('Publish To Nexus') {
            steps {
                withMaven(globalMavenSettingsConfig: 'global-settings', jdk: 'jdk17', maven: 'maven3', mavenSettingsConfig: '', traceability: true) {
                    sh "mvn deploy -DskipTests"
                }
            }
        }
        
        stage('Build Docker Image') {
            steps {
                script {
                    withDockerRegistry(credentialsId: 'nexus-credentials', toolName: 'docker') {
                        sh """
                            docker build -t ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:${APP_VERSION} .
                            docker tag ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:${APP_VERSION} ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:latest
                        """
                    }
                }
            }
        }
        
        stage('Docker Image Scan') {
            steps {
                sh "trivy image --format table -o trivy-image-report.html ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:latest"
                archiveArtifacts 'trivy-image-report.html'
            }
        }
        
        stage('Push Docker Image to Nexus') {
            steps {
                script {
                    withDockerRegistry(credentialsId: 'nexus-credentials', toolName: 'docker') {
                        sh """
                            docker push ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:${APP_VERSION}
                            docker push ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:latest
                        """
                    }
                }
            }
        }
        
        stage('Deploy To Kubernetes') {
            steps {
                withKubeConfig(
                    credentialsId: 'k8s-credentials', 
                    namespace: "${K8S_NAMESPACE}",
                    serverUrl: "${K8S_MASTER}"
                ) {
                    sh """
                        # Create namespace if not exists
                        kubectl create namespace ${K8S_NAMESPACE} --dry-run=client -o yaml | kubectl apply -f -
                        
                        # Update deployment with new image
                        kubectl set image deployment/boardgame-app boardgame-app=${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:${APP_VERSION} -n ${K8S_NAMESPACE} --record
                        
                        # If deployment doesn't exist, create it
                        kubectl apply -f k8s-deployment.yaml -n ${K8S_NAMESPACE} || true
                        kubectl apply -f k8s-service.yaml -n ${K8S_NAMESPACE} || true
                        kubectl apply -f k8s-ingress.yaml -n ${K8S_NAMESPACE} || true
                    """
                }
            }
        }
        
        stage('Verify Deployment') {
            steps {
                withKubeConfig(
                    credentialsId: 'k8s-credentials',
                    namespace: "${K8S_NAMESPACE}", 
                    serverUrl: "${K8S_MASTER}"
                ) {
                    sh """
                        echo "=== Checking Deployment Status ==="
                        kubectl rollout status deployment/boardgame-app -n ${K8S_NAMESPACE} --timeout=300s
                        
                        echo "=== Checking Pods ==="
                        kubectl get pods -n ${K8S_NAMESPACE} -l app=boardgame-app
                        
                        echo "=== Checking Services ==="
                        kubectl get svc -n ${K8S_NAMESPACE}
                        
                        echo "=== Checking Ingress ==="
                        kubectl get ingress -n ${K8S_NAMESPACE}
                    """
                }
            }
        }
        
        stage('Smoke Test') {
            steps {
                script {
                    // Wait for app to be ready and test basic functionality
                    sh """
                        sleep 30
                        echo "=== Performing Smoke Test ==="
                        # Get the application URL and test
                        kubectl get svc -n ${K8S_NAMESPACE}
                        echo "Application should be accessible via the service/ingress"
                    """
                }
            }
        }
    }
    post {
        always {
            script {
                // Enhanced email notification
                def jobName = env.JOB_NAME
                def buildNumber = env.BUILD_NUMBER
                def pipelineStatus = currentBuild.result ?: 'SUCCESS'
                def bannerColor = pipelineStatus == 'SUCCESS' ? 'green' : 'red'
                
                def body = """
                <html>
                <body>
                <div style="border: 4px solid ${bannerColor}; padding: 10px;">
                    <h2>${jobName} - Build ${buildNumber}</h2>
                    <div style="background-color: ${bannerColor}; padding: 10px;">
                        <h3 style="color: white;">Pipeline Status: ${pipelineStatus}</h3>
                    </div>
                    <p><strong>Application:</strong> BoardGame Java Application</p>
                    <p><strong>Version:</strong> ${APP_VERSION}</p>
                    <p><strong>Docker Image:</strong> ${NEXUS_REGISTRY}/docker-hosted/${DOCKER_IMAGE_NAME}:${APP_VERSION}</p>
                    <p><strong>Namespace:</strong> ${K8S_NAMESPACE}</p>
                    <p><strong>Test Credentials:</strong></p>
                    <ul>
                        <li>User: bugs / bunny</li>
                        <li>Manager: daffy / duck</li>
                    </ul>
                    <p>Check the <a href="${BUILD_URL}">console output</a> for details.</p>
                </div>
                </body>
                </html>
                """
                
                emailext (
                    subject: "BoardGame App - Build ${buildNumber} - ${pipelineStatus}",
                    body: body,
                    to: 'your-email@yourcompany.com',  // UPDATE: Your email
                    mimeType: 'text/html',
                    attachmentsPattern: 'trivy-fs-report.html,trivy-image-report.html'
                )
            }
        }
        
        success {
            script {
                echo "🎉 Pipeline completed successfully! BoardGame app is deployed."
                echo "📝 Test with: username: bugs | password: bunny (user role)"
                echo "📝 Test with: username: daffy | password: duck (manager role)"
            }
        }
        
        failure {
            script {
                echo "❌ Pipeline failed! Check the logs for details."
            }
        }
    }
}
